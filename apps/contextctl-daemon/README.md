# @contextctl/daemon

The `contextctl` command, MCP server, optional loopback HTTP adapter, and runtime composition for Contextctl.

See the [Contextctl README](https://github.com/TEAM-SEAM-contextctl/contextctl#readme) for installation, the five-minute walkthrough, configuration, and operational guidance.

```bash
contextctl --help
```
